dependencies:
  pre:
    - sudo apt-get install build-essential autoconf libgtk-3-dev libvte-2.91-dev liblua5.3-dev
test:
  pre:
    - autoreconf -fvi
    - ./configure
    - make
  override:
    - make check
